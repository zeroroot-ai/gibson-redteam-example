# Gibson plugin — generated by gibson-cli plugin init

# Host key — persisted by gibson-cli plugin enroll; never committed.
host_key
host_key.json
~/.gibson/plugin/findings-sink/

# Build artifacts
/findings-sink
/dist/

# Go toolchain
*.test
*.out
coverage.html

# Protocol Buffers generated code (regenerate via make proto)
gen/
